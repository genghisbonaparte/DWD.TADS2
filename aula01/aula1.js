var a = 10 // inteiro
var b = '20' // string
var c = false // 0 - variavel booleana
var d = true // 1


// nome de variável apenas com letras minuculas, nada de numeros, letras maiusculas, caracteres especiais, etc, a não ser no formato "var a = bannanaMaca"

// Quase nunca se usa o var


a = 'beto' // troca de tipagem  _ objeto string

let x = 20 // variáveis a nivel local

const y = 'paiva' // isso é uma constante

//_______________

var d // varável do tipo undefined
var e = '' // nonne | null 

var arr = new Array()

var arr2 = new Array(1,2,3,4) // cpntrução de um objeto

var arr3 = [1,2,3,4]

var arr4 = new Array(5) // pouco utilizado

console.log(arr3)

if(a >= 10){
    console.log('a maior que 10')
    let w = 30
}else {
    if(a <= 20){
        console.log('a menor que 20')
        let a = 20
    }
}

// estruturas condicionais
if (b == x) {
    console.log(x == b)
}else {
    console.log("diferentes")
}
